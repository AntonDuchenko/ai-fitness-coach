---
description: Local QA tester login for Browser MCP (localhost only)
alwaysApply: true
---

# QA tester (local dev only)

Use when **Browser MCP** hits a **login/auth wall** on **`http://localhost:3000`** (or paths under it) and the run would otherwise be BLOCKED.

- **Email:** `test@gmail.com`
- **Password:** `12345678Aa!`

**Scope:** local development / manual QA automation only. Do not use outside the intended test environment; rotate credentials if they are exposed.

After login, continue the same Stitch-vs-live checks for the requested screen.
